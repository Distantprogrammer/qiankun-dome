{
  "name": "qiankun-example",
  "version": "0.0.1",
  "description": "qiankun demo",
  "main": "index.js",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "install": "npm-run-all --serial install:*",
    "install:main": "cd main && npm i",
    "install:sub-monaco-editor": "cd sub-monaco-editor && npm i",
    "start": "npm-run-all --parallel start:*",
    "start:sub-monaco-editor": "cd sub-monaco-editor && npm start",
    "build": "npm-run-all build:* && bash ./scripts/bundle.sh",
    "build:sub-monaco-editor": "cd sub-monaco-editor && npm run build",
    "build:main": "cd main && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@fengxianqi.github.com:fengxianqi/qiankun-example.git"
  },
  "keywords": [
    "qiankun",
    "qiankun-example",
    "demo"
  ],
  "author": "fengxianqi",
  "license": "MIT"
}
